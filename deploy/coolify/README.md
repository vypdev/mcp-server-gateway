# Deprecated Coolify deployment reference

This directory is retained only to avoid losing the original deployment notes. Do not use it for the Lab01 host-control deployment.

The supported production path is the native systemd service in `deploy/systemd/` and `docs/native-deployment.md`.
